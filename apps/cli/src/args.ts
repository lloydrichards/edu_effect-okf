import { Argument } from "effect/unstable/cli";

export const bundlePath = Argument.string("bundlePath");
export const conceptId = Argument.string("conceptId");
export const query = Argument.string("query");
